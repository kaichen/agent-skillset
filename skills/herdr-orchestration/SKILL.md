---
name: herdr-orchestration
description: Use Herdr to orchestrate multi agents. Triggers on "call agent via herdr", "lauch agents for", "启动 herdr codex", "呼叫 herdr codex", or when user wants launch new agent.
---

Invoke /herdr first. Alway use tiled layout (vertical split first, then horizontal; max of 3x3 panes/tab, overflow to new tabs). Group agents by purpose per tab, name tab as role, name pane as agent. Pin the orchestrator to Tab 1's top-left pane.
